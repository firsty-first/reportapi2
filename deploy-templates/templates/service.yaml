apiVersion: v1
kind: Service
metadata:
  name: {{ include "reportgeneration2.fullname" . }}
  labels:
    {{- include "reportgeneration2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reportgeneration2.selectorLabels" . | nindent 4 }}
